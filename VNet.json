{
  "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentTemplate.json#",
  "contentVersion": "1.0.0.0",
  "metadata": {
    "_generator": {
      "name": "bicep",
      "version": "0.6.18.56646",
      "templateHash": "8826741834871630128"
    }
  },
  "parameters": {
    "vnet_Name": {
      "type": "string"
    },
    "vnet_AddressSpace": {
      "type": "string"
    },
    "vnet_Location": {
      "type": "string"
    },
    "vnet_RouteTableId": {
      "type": "string"
    },
    "subnets": {
      "type": "array"
    }
  },
  "variables": {
    "specialSubnets": [
      "GatewaySubnet",
      "AzureFirewallSubnet",
      "AzureBastionSubnet",
      "RouteServerSubnet"
    ]
  },
  "resources": [
    {
      "type": "Microsoft.Network/virtualNetworks",
      "apiVersion": "2020-11-01",
      "name": "[parameters('vnet_Name')]",
      "location": "[parameters('vnet_Location')]",
      "tags": "[resourceGroup().tags]",
      "properties": {
        "copy": [
          {
            "name": "subnets",
            "count": "[length(parameters('subnets'))]",
            "input": {
              "name": "[parameters('subnets')[copyIndex('subnets')].name]",
              "properties": {
                "addressPrefix": "[parameters('subnets')[copyIndex('subnets')].addressSpace]",
                "networkSecurityGroup": {
                  "id": "[if(contains(variables('specialSubnets'), parameters('subnets')[copyIndex('subnets')].name), null(), resourceId('Microsoft.Network/networkSecurityGroups', parameters('subnets')[range(0, length(parameters('subnets')))[copyIndex('subnets')]].nsgName))]"
                },
                "routeTable": {
                  "id": "[if(contains(variables('specialSubnets'), parameters('subnets')[copyIndex('subnets')].name), null(), parameters('vnet_RouteTableId'))]"
                }
              }
            }
          }
        ],
        "addressSpace": {
          "addressPrefixes": [
            "[parameters('vnet_AddressSpace')]"
          ]
        }
      },
      "dependsOn": [
        "networkSecurityGroup"
      ]
    },
    {
      "condition": "[not(equals(parameters('subnets')[range(0, length(parameters('subnets')))[copyIndex()]].nsgName, 'none'))]",
      "copy": {
        "name": "networkSecurityGroup",
        "count": "[length(range(0, length(parameters('subnets'))))]"
      },
      "type": "Microsoft.Network/networkSecurityGroups",
      "apiVersion": "2020-11-01",
      "name": "[parameters('subnets')[range(0, length(parameters('subnets')))[copyIndex()]].nsgName]",
      "location": "[parameters('vnet_Location')]",
      "tags": "[resourceGroup().tags]",
      "properties": {
        "securityRules": []
      }
    }
  ]
}